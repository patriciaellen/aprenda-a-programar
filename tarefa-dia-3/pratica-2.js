let numeroInicial = 1
let numeroFinal = 10 
let numeroTabuada = 9

while(numeroInicial <= numeroFinal){
    console.log(`${numeroTabuada} x ${numeroInicial} = ${numeroTabuada*numeroInicial}`) 
    numeroInicial = numeroInicial + 1 
}